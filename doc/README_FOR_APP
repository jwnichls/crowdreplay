TODO:

* Capistrano deployment
   - create the appropriate directories and configuration on server
* Improved UI for navigating around the application
* omniauth twitter for recorder tokens
* Record the actual tweets into a file instead of the database (just store basic meta-data in the database)
   - Probably store the tweets in per minute files
